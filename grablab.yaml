session_name: grablab
windows:
  - window_name: code
    focus: true
    start-directory: ./
    panes:
      - shell_command:
        - workong django
        - emacs
  - window_name: cmd
    start-directory: ./
    panes:
      - shell_command:
        - workon django
        - clear
  - window_name: front
    panes:
      - shell_command:
        - clear
  - window_name: back
    panes:
      - shell_command:
        - workon django
        - clear
  - window_name: db
    panes:
      - shell_command:
        - psql grablab
